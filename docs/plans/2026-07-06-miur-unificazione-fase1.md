# Unificazione MIUR — Fase 1 (fondamenta) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Sostituire le swing tables `new_adozioni`/`import_adozioni`/`old_adozioni`/`new_scuole` con `miur_adozioni`/`miur_scuole` partizionate per `anno_scolastico`, log import in `miur_import_runs`, e migrare tutti i consumer.

**Architecture:** Tabelle partizionate LIST su `anno_scolastico`; import blue-green con DETACH/ATTACH di partizione (niente più drop/recreate matview). Migrazione incrementale: prima le tabelle nuove + backfill, poi le matview, poi il ponte di **viste SQL di compatibilità** con i vecchi nomi (drop tabelle → viste omonime), infine i consumer a gruppi e il drop delle viste. Ogni task lascia la suite verde ed è committabile.

**Tech Stack:** Rails 8.1, PostgreSQL (declarative partitioning), activerecord-import, Minitest + Mocha. Docker: ogni comando Rails va eseguito con `docker exec prova-app-1 …`.

**Design di riferimento:** `docs/plans/2026-07-06-miur-unificazione-design.md`

**Prerequisiti verificati (2026-07-06):**
- Scheduler sidekiq-cron già bloccato dall'utente (nessun import concorrente).
- Dev allineato al backup prod: `new_adozioni` 3.387.137 righe (202627), `import_adozioni` 3.539.617 (202526), `old_adozioni` 3.523.021 (202425), `new_scuole` 62.850 (202627).
- FK reali da gestire: `adozioni→import_adozioni`, `adozioni_comunicate→import_adozioni` (colonne id-only, l'associazione non è mai letta come oggetto), `old_adozioni→import_scuole` (cade col drop), `scuole/user_scuole→import_scuole` (import_scuole RESTA).
- Viste DB dipendenti dalle tabelle da droppare: `mercato_nazionale_libri`, `mercato_nazionale_mercati`, `mercato_scuola_mercati` (matview, si ridefiniscono), `view_classi` (usata da Views::Classe in chips/classi controller), `view_adozioni_elementari`, `view_adozioni144ant_editori` (da verificare in Blazer, probabilmente morte).
- Fixture esistenti: `test/fixtures/new_adozioni.yml`, `test/fixtures/new_scuole.yml`.

**Regole trasversali:**
- NON toccare `import_scuole` (tabella, modello, geocoding): resta com'è (Fase 4 futura).
- NON toccare `app/services/miur/*_scraper.rb` se non dove indicato (Task 8): i runbook CLAUDE.md li referenziano.
- Test: `docker exec prova-app-1 bin/rails test <file>` per il singolo file, `docker exec prova-app-1 bin/rails test` per la suite (gate). Il DB dev pesa ~10M righe MIUR: i task di backfill girano in minuti, non secondi.
- Commit frequenti, messaggio per ogni task indicato in fondo al task.

---

## Task 1: Passa a structure.sql

Lo schema dumper di Rails non rappresenta il partitioning dichiarativo: con `schema.rb` il `db:schema:load` di test/CI ricostruirebbe tabelle sbagliate. Va fatto PRIMA di creare le tabelle partizionate.

**Files:**
- Modify: `config/application.rb`
- Create: `db/structure.sql` (generato)
- Delete: `db/schema.rb` (dopo il dump)

**Step 1:** In `config/application.rb`, dentro `class Application < Rails::Application`, aggiungi:

```ruby
config.active_record.schema_format = :sql
```

**Step 2:** Genera il dump e rimuovi schema.rb:

```bash
docker exec prova-app-1 bin/rails db:schema:dump
ls -la db/structure.sql        # deve esistere, ~200KB+
git rm db/schema.rb
```

**Step 3:** Verifica che il DB di test si ricostruisca dal nuovo formato:

```bash
docker exec prova-app-1 bin/rails db:test:prepare
docker exec prova-app-1 bin/rails test test/models/controllo_adozioni/anteprima_test.rb
```
Expected: PASS (5 runs).

**Step 4: Commit**

```
build(db): Passa a structure.sql per supportare il partitioning

schema.rb non sa rappresentare PARTITION BY: le tabelle miur_*
partizionate in arrivo verrebbero ricostruite sbagliate da
db:schema:load in test e CI.
```

---

## Task 2: Migration tabelle partizionate + miur_import_runs

**Files:**
- Create: `db/migrate/XXXX_create_miur_tables.rb` (`docker exec prova-app-1 bin/rails generate migration CreateMiurTables`)

**Step 1:** Scrivi la migration. Tutto raw SQL: il DSL Rails non supporta il partitioning. PK composita `(anno_scolastico, id)` (la partition key DEVE stare in ogni unique). Le colonne replicano `new_adozioni`/`new_scuole` (minuscole).

```ruby
class CreateMiurTables < ActiveRecord::Migration[8.1]
  ANNI = %w[202425 202526 202627].freeze

  def up
    execute <<~SQL
      CREATE TABLE miur_adozioni (
        id bigint GENERATED BY DEFAULT AS IDENTITY,
        anno_scolastico varchar NOT NULL,
        annocorso varchar, autori varchar, codiceisbn varchar, codicescuola varchar,
        combinazione varchar, consigliato varchar, daacquist varchar, disciplina varchar,
        editore varchar, nuovaadoz varchar, prezzo varchar, sezioneanno varchar,
        sottotitolo varchar, tipogradoscuola varchar, titolo varchar, volume varchar,
        import_scuola_id bigint,
        PRIMARY KEY (anno_scolastico, id)
      ) PARTITION BY LIST (anno_scolastico)
    SQL
    execute <<~SQL
      CREATE UNIQUE INDEX index_miur_adozioni_on_classe ON miur_adozioni
        (anno_scolastico, codicescuola, annocorso, sezioneanno, combinazione, codiceisbn, disciplina)
    SQL
    execute "CREATE INDEX idx_miur_adoz_ee ON miur_adozioni (codicescuola) INCLUDE (editore, annocorso, disciplina) WHERE tipogradoscuola = 'EE'"
    execute "CREATE INDEX idx_miur_adozioni_codicescuola ON miur_adozioni (codicescuola)"
    execute "CREATE INDEX idx_miur_adozioni_disc_anno_tg ON miur_adozioni (disciplina, annocorso, tipogradoscuola)"

    execute <<~SQL
      CREATE TABLE miur_scuole (
        id bigint GENERATED BY DEFAULT AS IDENTITY,
        anno_scolastico varchar NOT NULL,
        area_geografica varchar, cap varchar, codice_comune varchar,
        codice_istituto_riferimento varchar, codice_scuola varchar, comune varchar,
        denominazione varchar, denominazione_istituto_riferimento varchar,
        descrizione_caratteristica varchar, email varchar,
        indicazione_sede_direttivo varchar, indicazione_sede_omnicomprensivo varchar,
        indirizzo varchar, pec varchar, provincia varchar, regione varchar,
        sede_scolastica varchar, sito_web varchar, tipo_scuola varchar,
        import_scuola_id bigint,
        PRIMARY KEY (anno_scolastico, id)
      ) PARTITION BY LIST (anno_scolastico)
    SQL
    execute "CREATE UNIQUE INDEX index_miur_scuole_on_codice_scuola ON miur_scuole (anno_scolastico, codice_scuola)"
    execute "CREATE INDEX idx_miur_scuole_cod ON miur_scuole (codice_scuola) INCLUDE (regione, provincia)"
    execute "CREATE INDEX idx_miur_scuole_tipo ON miur_scuole (tipo_scuola)"

    ANNI.each do |anno|
      execute "CREATE TABLE miur_adozioni_#{anno} PARTITION OF miur_adozioni FOR VALUES IN ('#{anno}')"
    end
    execute "CREATE TABLE miur_scuole_202627 PARTITION OF miur_scuole FOR VALUES IN ('202627')"

    create_table :miur_import_runs do |t|
      t.string :anno_scolastico, null: false
      t.string :dataset, null: false, default: "adozioni" # adozioni | scuole
      t.integer :righe_totali
      t.integer :delta_righe
      t.jsonb :regioni_aggiornate, default: []
      t.jsonb :regioni_stale, default: []
      t.jsonb :regioni_fallite, default: []
      t.datetime :completed_at
      t.timestamps
      t.index [:dataset, :completed_at]
    end
  end

  def down
    drop_table :miur_import_runs
    execute "DROP TABLE miur_adozioni CASCADE"
    execute "DROP TABLE miur_scuole CASCADE"
  end
end
```

**Step 2:** `docker exec prova-app-1 bin/rails db:migrate` — Expected: OK, poi verifica:

```bash
docker exec prova-db-1 psql -U prova -d prova_development -c "\d+ miur_adozioni" | head -30
```
Expected: `Partition key: LIST (anno_scolastico)` e 3 partizioni elencate.

**Step 3:** `docker exec prova-app-1 bin/rails db:test:prepare` — la structure.sql deve ricreare tutto anche in test.

**Step 4: Commit** (`db/migrate/…`, `db/structure.sql`)

```
feat(miur): Tabelle miur_adozioni/miur_scuole partizionate + miur_import_runs

Fase 1 del design 2026-07-06: una tabella per i dati ministeriali con
anno_scolastico come partition key al posto delle swing tables
new_/import_/old_. PK composita (anno_scolastico, id) come richiede il
partitioning; indici identici a quelli di new_adozioni.
```

---

## Task 3: Modelli Miur::Adozione, Miur::Scuola, Miur::ImportRun (TDD)

**Files:**
- Create: `app/models/miur.rb`, `app/models/miur/adozione.rb`, `app/models/miur/scuola.rb`, `app/models/miur/import_run.rb`
- Create: `test/models/miur/adozione_test.rb`
- Create: `test/fixtures/miur/adozioni.yml`, `test/fixtures/miur/scuole.yml` (copie adattate di `new_adozioni.yml`/`new_scuole.yml` con `anno_scolastico` esplicito)

**Step 1:** Test prima:

```ruby
# test/models/miur/adozione_test.rb
require "test_helper"

class Miur::AdozioneTest < ActiveSupport::TestCase
  test "per_anno filtra sulla partizione" do
    assert Miur::Adozione.per_anno("202627").all? { |a| a.anno_scolastico == "202627" }
  end

  test "anno_corrente deriva dal massimo di miur_scuole" do
    assert_equal Miur::Scuola.maximum(:anno_scolastico), Miur.anno_corrente
  end

  test "escluso_dal_tetto? per alternativa e parascolastica" do
    assert Miur::Adozione.new(disciplina: "ADOZIONE ALTERNATIVA ALLA RELIGIONE C.").escluso_dal_tetto?
    refute Miur::Adozione.new(disciplina: "ITALIANO").escluso_dal_tetto?
  end

  test "prezzo_euro converte la stringa MIUR" do
    assert_equal BigDecimal("12.34"), Miur::Adozione.new(prezzo: "12,34").prezzo_euro
    assert_nil Miur::Adozione.new(prezzo: "ND").prezzo_euro
  end
end
```

**Step 2:** Run: FAIL (`uninitialized constant Miur::Adozione`).

**Step 3:** Implementazione minima:

```ruby
# app/models/miur.rb
module Miur
  def self.table_name_prefix = "miur_"

  # Anno campagna corrente: il massimo pubblicato nell'anagrafe scuole.
  # (Fase 2 lo promuove a value object AnnoScolastico.)
  def self.anno_corrente
    Miur::Scuola.maximum(:anno_scolastico)
  end
end

# app/models/miur/adozione.rb
class Miur::Adozione < ApplicationRecord
  scope :per_anno, ->(anno) { where(anno_scolastico: anno) }
  scope :correnti, -> { per_anno(Miur.anno_corrente) }

  # (porta qui escluso_dal_tetto? e prezzo_euro da NewAdozione, identici)
end

# app/models/miur/scuola.rb
class Miur::Scuola < ApplicationRecord
  scope :per_anno, ->(anno) { where(anno_scolastico: anno) }
  belongs_to :anagrafe, class_name: "ImportScuola", foreign_key: :import_scuola_id, optional: true
end

# app/models/miur/import_run.rb
class Miur::ImportRun < ApplicationRecord
  scope :adozioni, -> { where(dataset: "adozioni") }
  scope :scuole,   -> { where(dataset: "scuole") }
end
```

Nessuna validazione di uniqueness AR su Miur::Adozione (la garantisce l'indice; il load è bulk via activerecord-import). NON aggiungere metodi titleize né override di attributi (lezione ImportAdozione#editore).

**Step 4:** Run test: PASS. **Step 5: Commit** `feat(miur): Modelli Miur::Adozione/Scuola/ImportRun`

---

## Task 4: Backfill

**Files:**
- Create: `lib/tasks/miur.rake` (nuovo file, ospiterà tutti i task `miur:*`)

**Step 1:** Task idempotente (TRUNCATE partizione + INSERT SELECT). Gli id ORIGINALI si preservano — `adozioni.import_adozione_id`/`adozioni_comunicate.import_adozione_id` restano risolvibili nella partizione 202526. Identity `BY DEFAULT` accetta id espliciti.

```ruby
namespace :miur do
  desc "Backfill one-shot delle swing tables in miur_adozioni/miur_scuole"
  task backfill: :environment do
    conn = ActiveRecord::Base.connection

    {"202425" => "old_adozioni", "202627" => "new_adozioni"}.each do |anno, tab|
      conn.execute("TRUNCATE miur_adozioni_#{anno}")
      conn.execute(<<~SQL)
        INSERT INTO miur_adozioni
          (id, anno_scolastico, annocorso, autori, codiceisbn, codicescuola, combinazione,
           consigliato, daacquist, disciplina, editore, nuovaadoz, prezzo, sezioneanno,
           sottotitolo, tipogradoscuola, titolo, volume, import_scuola_id)
        SELECT id, '#{anno}', annocorso, autori, codiceisbn, codicescuola, combinazione,
               consigliato, daacquist, disciplina, editore, nuovaadoz, prezzo, sezioneanno,
               sottotitolo, tipogradoscuola, titolo, volume, import_scuola_id
        FROM #{tab}
      SQL
      puts "#{anno}: #{conn.select_value("SELECT count(*) FROM miur_adozioni_#{anno}")} righe da #{tab}"
    end

    conn.execute("TRUNCATE miur_adozioni_202526")
    conn.execute(<<~SQL)
      INSERT INTO miur_adozioni
        (id, anno_scolastico, annocorso, autori, codiceisbn, codicescuola, combinazione,
         consigliato, daacquist, disciplina, editore, nuovaadoz, prezzo, sezioneanno,
         sottotitolo, tipogradoscuola, titolo, volume)
      SELECT id, '202526', "ANNOCORSO", "AUTORI", "CODICEISBN", "CODICESCUOLA", "COMBINAZIONE",
             "CONSIGLIATO", "DAACQUIST", "DISCIPLINA", "EDITORE", "NUOVAADOZ", "PREZZO",
             "SEZIONEANNO", "SOTTOTITOLO", "TIPOGRADOSCUOLA", "TITOLO", "VOLUME"
      FROM import_adozioni
    SQL
    puts "202526: #{conn.select_value("SELECT count(*) FROM miur_adozioni_202526")} righe da import_adozioni"

    conn.execute("TRUNCATE miur_scuole_202627")
    conn.execute(<<~SQL)
      INSERT INTO miur_scuole
        (id, anno_scolastico, area_geografica, cap, codice_comune, codice_istituto_riferimento,
         codice_scuola, comune, denominazione, denominazione_istituto_riferimento,
         descrizione_caratteristica, email, indicazione_sede_direttivo,
         indicazione_sede_omnicomprensivo, indirizzo, pec, provincia, regione,
         sede_scolastica, sito_web, tipo_scuola, import_scuola_id)
      SELECT id, anno_scolastico, area_geografica, cap, codice_comune, codice_istituto_riferimento,
             codice_scuola, comune, denominazione, denominazione_istituto_riferimento,
             descrizione_caratteristica, email, indicazione_sede_direttivo,
             indicazione_sede_omnicomprensivo, indirizzo, pec, provincia, regione,
             sede_scolastica, sito_web, tipo_scuola, import_scuola_id
      FROM new_scuole
    SQL

    %w[miur_adozioni miur_scuole].each do |t|
      max = conn.select_value("SELECT COALESCE(MAX(id),1) FROM #{t}")
      conn.execute("SELECT setval(pg_get_serial_sequence('#{t}', 'id'), #{max})")
      conn.execute("ANALYZE #{t}")
    end
    puts "Backfill completato"
  end
end
```

**Step 2:** Esegui e verifica i conteggi contro le sorgenti:

```bash
docker exec prova-app-1 bin/rails miur:backfill
docker exec prova-db-1 psql -U prova -d prova_development -c \
  "SELECT anno_scolastico, count(*) FROM miur_adozioni GROUP BY 1 ORDER BY 1;"
```
Expected: 202425=3523021, 202526=3539617, 202627=3387137 (numeri del backup 2026-07-06; devono essere IDENTICI ai count delle tabelle sorgente al momento del run).

**Step 3: Commit** `feat(miur): Task miur:backfill dalle swing tables (id preservati)`

---

## Task 5: Matview su miur_adozioni (via UNION)

Le matview devono smettere di dipendere dalle vecchie tabelle PRIMA del loro drop (Task 9). Con miur_adozioni popolata, l'UNION import+new si riduce a una SELECT sola.

**Files:**
- Create: `db/migrate/XXXX_repoint_mercato_matviews_to_miur_adozioni.rb`

**Step 1:** Migration `up`: DROP delle 3 matview e CREATE con la nuova definizione. Per ciascuna, la definizione è quella attuale (vedi `lib/tasks/import.rake:574-641`) con la subquery UNION sostituita da:

```sql
SELECT anno_scolastico, tipogradoscuola AS tipo_grado_scuola, disciplina,
       annocorso AS anno_corso, codiceisbn AS codice_isbn,
       codicescuola || '_' || annocorso || '_' || sezioneanno AS sezione_key
FROM miur_adozioni
WHERE daacquist = 'Si'
```

(adattando le colonne esposte per ognuna delle 3 come nelle definizioni correnti; `anno_scolastico IS NOT NULL` non serve più: è NOT NULL per schema). Ricrea gli stessi indici unique/regular (`idx_mercato_naz_libri_pk`, `idx_mercato_naz_mercati_pk`, `idx_mercato_scuola_mercati_pk`, `idx_mercato_scuola_mercati_scuola`). Il `down` ripristina le definizioni UNION attuali.

**Step 2:** `db:migrate`, poi verifica di equivalenza (deve dare 0 righe di differenza sui totali):

```bash
docker exec prova-db-1 psql -U prova -d prova_development -c \
  "SELECT anno_scolastico, sum(sezioni) FROM mercato_nazionale_mercati GROUP BY 1 ORDER BY 1;"
```
Expected: valori per 202425 (nuovo!), 202526 e 202627. Confronta 202526/202627 con i valori pre-migrazione (annotali prima).

**Step 3:** Verifica che `AdozioniAnalytics.refresh_rollup!` funzioni ancora:

```bash
docker exec prova-app-1 bin/rails runner 'AdozioniAnalytics.refresh_rollup!; puts "ok"'
```

**Step 4: Commit** `feat(miur): Matview mercato_* lette da miur_adozioni (via UNION import+new)`

---

## Task 6: Task miur:importa_adozioni con swap di partizione

**Files:**
- Modify: `lib/tasks/miur.rake`
- Reference: `lib/tasks/import.rake:413-652` (struttura da portare), `:661-797` (per scuole)

**Step 1:** Nuovo task, stessa ossatura di `import:new_adozioni` (soglia CSV, advisory lock `ADOZIONI_LOCK_KEY`, load CSV in staging con activerecord-import, dedup ROW_NUMBER, indici, ANALYZE) con queste differenze:

1. Staging: `CREATE TABLE miur_adozioni_stg (LIKE miur_adozioni INCLUDING DEFAULTS)` — tabella normale, non partizione. Modello staging `MiurAdozioneStaging` via `const_set` come oggi.
2. L'anno viene da `Miur.anno_corrente` (fallback data come oggi, vedi `import.rake:477-480`).
3. Indici staging: PK `(anno_scolastico, id)` + gli stessi 4, con nomi `_stg`.
4. CHECK per attach senza full-scan: `ALTER TABLE miur_adozioni_stg ADD CONSTRAINT stg_anno CHECK (anno_scolastico = '<anno>')`.
5. Swap (al posto del blocco `import.rake:547-641` — niente matview!):

```ruby
part = "miur_adozioni_#{anno}"
conn.transaction do
  if conn.select_value("SELECT to_regclass('#{part}')")
    conn.execute("ALTER TABLE miur_adozioni DETACH PARTITION #{part}")
    conn.execute("DROP TABLE #{part}")
  end
  conn.execute("ALTER TABLE miur_adozioni ATTACH PARTITION miur_adozioni_stg FOR VALUES IN ('#{anno}')")
  conn.execute("ALTER TABLE miur_adozioni_stg RENAME TO #{part}")
  conn.execute("ALTER TABLE #{part} DROP CONSTRAINT stg_anno")
end
```

6. Dopo lo swap, registra il run:

```ruby
prev = Miur::ImportRun.adozioni.where(anno_scolastico: anno).order(:completed_at).last
Miur::ImportRun.create!(
  dataset: "adozioni", anno_scolastico: anno,
  righe_totali: totale, delta_righe: prev ? totale - prev.righe_totali : nil,
  completed_at: Time.current
)
```

7. In coda restano `RefreshMercatoNazionaleRollupJob.perform_later` (ora è il refresh VERO, non più rete di sicurezza) e `RicalcolaAnomalieJob.perform_later`.

Aggiungi anche `miur:importa_scuole` (stessa trasformazione partendo da `import:new_scuole`, partizione `miur_scuole_<anno>`, mantiene l'UPDATE post-swap di `import_scuola_id` dal join su `import_scuole`) e `miur:cambia_religione` (copia di `import:cambia_religione` su `Miur::Adozione.correnti`).

I task vecchi in `import.rake` NON si toccano ancora (li usa lo scraper fino al Task 8).

**Step 2:** Test end-to-end in dev (i 20 CSV del 02/07 sono già in `tmp/_miur/adozioni/`):

```bash
docker exec prova-app-1 bin/rails miur:importa_adozioni 2>&1 | tail -5
```
Expected: `Swap completato: miur_adozioni_202627 ha 3387137 righe` (±0 rispetto al backfill: stessi CSV), 5 duplicati rimossi, un record in `miur_import_runs` con `delta_righe` 0.

```bash
docker exec prova-db-1 psql -U prova -d prova_development -c \
  "SELECT dataset, anno_scolastico, righe_totali, delta_righe FROM miur_import_runs;"
```

**Step 3: Commit** `feat(miur): Import blue-green con swap di partizione + log in miur_import_runs`

---

## Task 7: (gate intermedio) Suite completa

```bash
docker exec prova-app-1 bin/rails test
```
Expected: verde (finora abbiamo solo AGGIUNTO — nessun consumer è cambiato). Se qualcosa è rosso, fermati e sistemalo prima di procedere: da qui in poi si modifica il mondo esistente.

---

## Task 8: Scrapers → task miur:*

**Files:**
- Modify: `app/services/miur/adozioni_scraper.rb` (`process_imports`, ~riga 189-209; costante `MIN_CSV_FOR_IMPORT` resta qui, riga 17)
- Modify: `app/services/miur/scuole_scraper.rb` (`process_imports`, ~riga 202-216)
- Modify: `lib/tasks/miur.rake`

**Step 1:** In `AdozioniScraper#process_imports` sostituisci la catena `import:new_adozioni` → `import:cambia_religione` con `miur:importa_adozioni` → `miur:cambia_religione` (il `controllo_adozioni:rebuild` resta). In `ScuoleScraper#process_imports`: `import:new_scuole` → `miur:importa_scuole`.

**Step 2:** Nel task `miur:importa_adozioni`, sostituisci il literal `min_csv_threshold = 18` con `Miur::AdozioniScraper::MIN_CSV_FOR_IMPORT` (unica fonte). Idem `miur:importa_scuole` con `Miur::ScuoleScraper::MIN_CSV_FOR_IMPORT`.

**Step 3:** Dopo `process_imports`, lo scraper aggancia gli esiti regione all'ultimo run:

```ruby
Miur::ImportRun.adozioni.order(:completed_at).last&.update!(
  regioni_aggiornate: regioni_aggiornate, regioni_stale: regioni_stale,
  regioni_fallite: regioni_fallite
)
```

**Step 4:** Verifica: `docker exec prova-app-1 bin/rails runner 'AdozioniScraperJob.new.perform'` — Expected: "Nessuna regione aggiornata o nuova, skip import" (MIUR fermo al 02/07) senza errori.

**Step 5: Commit** `feat(miur): Scrapers invocano i task miur:* e loggano gli esiti regione`

---

## Task 9: Drop tabelle vecchie + viste ponte

Il cuore della migrazione incrementale: le tabelle spariscono, al loro posto VISTE con gli stessi nomi e colonne. Tutti i consumer continuano a funzionare senza saperlo.

**Files:**
- Create: `db/migrate/XXXX_replace_swing_tables_with_bridge_views.rb`

**Step 1:** PRIMA di scrivere la migration, salva le definizioni delle viste legacy:

```bash
docker exec prova-db-1 psql -U prova -d prova_development -c "SELECT pg_get_viewdef('view_classi', true);" > /tmp/view_classi.sql
docker exec prova-db-1 psql -U prova -d prova_development -c "SELECT pg_get_viewdef('view_adozioni_elementari', true);"
docker exec prova-db-1 psql -U prova -d prova_development -c "SELECT pg_get_viewdef('view_adozioni144ant_editori', true);"
```

Verifica in Blazer (o chiedi all'utente) se `view_adozioni_elementari` e `view_adozioni144ant_editori` sono usate: nel codice app NON lo sono (grep 2026-07-06 a vuoto). Se inutilizzate → si droppano e basta; altrimenti si ricreano come `view_classi`.

**Step 2:** Migration `up`:

```ruby
def up
  # 1. FK verso import_adozioni: le colonne restano (riferimenti storici id-only,
  #    risolvibili nella partizione 202526 dove gli id sono preservati)
  remove_foreign_key :adozioni, :import_adozioni
  remove_foreign_key :adozioni_comunicate, :import_adozioni

  # 2. Viste legacy dipendenti
  execute "DROP VIEW IF EXISTS view_classi"
  execute "DROP VIEW IF EXISTS view_adozioni_elementari"        # morta (verificato Step 1)
  execute "DROP VIEW IF EXISTS view_adozioni144ant_editori"     # morta (verificato Step 1)

  # 3. Drop tabelle (old_adozioni si porta via la sua FK verso import_scuole)
  execute "DROP TABLE new_adozioni"
  execute "DROP TABLE IF EXISTS new_adozioni_stg"
  execute "DROP TABLE old_adozioni"
  execute "DROP TABLE import_adozioni"
  execute "DROP TABLE new_scuole"

  # 4. Viste ponte con i vecchi nomi
  execute <<~SQL
    CREATE VIEW new_adozioni AS
    SELECT * FROM miur_adozioni
    WHERE anno_scolastico = (SELECT max(anno_scolastico) FROM miur_scuole)
  SQL
  execute <<~SQL
    CREATE VIEW new_scuole AS
    SELECT * FROM miur_scuole
    WHERE anno_scolastico = (SELECT max(anno_scolastico) FROM miur_scuole)
  SQL
  execute <<~SQL
    CREATE VIEW import_adozioni AS
    SELECT id,
           codicescuola    AS "CODICESCUOLA",    annocorso   AS "ANNOCORSO",
           sezioneanno     AS "SEZIONEANNO",     combinazione AS "COMBINAZIONE",
           tipogradoscuola AS "TIPOGRADOSCUOLA", disciplina  AS "DISCIPLINA",
           codiceisbn      AS "CODICEISBN",      autori      AS "AUTORI",
           titolo          AS "TITOLO",          sottotitolo AS "SOTTOTITOLO",
           volume          AS "VOLUME",          editore     AS "EDITORE",
           prezzo          AS "PREZZO",          nuovaadoz   AS "NUOVAADOZ",
           daacquist       AS "DAACQUIST",       consigliato AS "CONSIGLIATO",
           anno_scolastico,
           NULL::timestamp AS created_at, NULL::timestamp AS updated_at
    FROM miur_adozioni
    WHERE anno_scolastico = '202526'
  SQL

  # 5. view_classi ricreata IDENTICA (ora legge attraverso la vista ponte)
  execute <<~SQL
    CREATE VIEW view_classi AS
    #{File.read(Rails.root.join('db/views_legacy/view_classi.sql'))}
  SQL
end
```

⚠️ Prima del commit copia la definizione salvata allo Step 1 in `db/views_legacy/view_classi.sql` (nuova directory). Nota colonne `import_adozioni`: verifica con `\d import_adozioni` PRIMA del drop che l'elenco alias della vista copra tutte le colonne effettivamente lette dai consumer (`geocoded`? no, quello è import_scuole). Se il modello ImportAdozione seleziona colonne mancanti i test lo urleranno al gate.

Il `down`: irreversibile senza i dati (raise `ActiveRecord::IrreversibleMigration`).

**Step 3:** `db:migrate`, poi **GATE: suite completa**:

```bash
docker exec prova-app-1 bin/rails test
```
Expected: verde. Le fixture `new_adozioni.yml`/`new_scuole.yml` ora inseriscono ATTRAVERSO le viste ponte: le viste semplici sono auto-updatable MA `new_adozioni`/`new_scuole` hanno una subquery nel WHERE → Postgres le considera comunque updatable per INSERT (la subquery è solo nel filtro); se il load fixture fallisce, la soluzione è rinominare subito le fixture in `miur/adozioni.yml`+`miur/scuole.yml` (già create nel Task 3) e aggiornare i test che le referenziano — fallo in questo stesso task se necessario.

**Step 4: Commit** `feat(miur)!: Drop swing tables, viste ponte di compatibilità`

---

## Task 10: Migra ControlloAdozioni::Rebuild + PrezzoMinisteriale

**Files:**
- Modify: `app/models/controllo_adozioni/rebuild.rb` (SQL raw: `new_adozioni` → `miur_adozioni` + filtro `anno_scolastico = :anno`; `new_scuole` → `miur_scuole` idem)
- Modify: `app/models/prezzo_ministeriale.rb` (`popola_da_new_adozioni!` e `popola_da_import_adozioni!` si fondono in `popola!(anno:)` su miur_adozioni; i chiamanti: `lib/tasks/controllo_adozioni.rake`)
- Test: esistenti (`test/models/controllo_adozioni/`, cerca i test di Rebuild) + run `RicalcolaAnomalieJob` in dev

**Procedura per OGNI file di questo e dei prossimi task consumer:** (1) grep `new_adozioni|new_scuole|import_adozioni|NewAdozione|NewScuola|ImportAdozione` nel file; (2) sostituisci tabella/modello e AGGIUNGI SEMPRE il filtro anno esplicito (`anno_scolastico = X`) — le viste ponte lo facevano implicitamente, `miur_adozioni` no: una query senza filtro anno legge 10M righe di 3 anni; (3) run dei test del file; (4) avanti.

Per Rebuild: l'anno è già passato come parametro (`anno_prezzi`); ogni INSERT deve filtrare `anno_scolastico = :anno`. Approfitta per popolare `controllo_anomalie.anno_scolastico` SOLO SE banale (una colonna in più negli INSERT) — altrimenti lascialo alla Fase 2 come da design.

**Verifica:** `docker exec prova-app-1 bin/rails runner 'ControlloAdozioni::Rebuild.run!; puts ControlloAnomalia.count'` — Expected: ~12327 (stesso ordine di grandezza di prima).

**Commit** `refactor(miur): Rebuild e PrezzoMinisteriale leggono miur_adozioni`

---

## Task 11: Migra Panoramica, PassaggioAnno, Dashboard

**Files:**
- Modify: `app/models/controllo_adozioni/panoramica.rb` (query su new_scuole:99, new_adozioni:120,158,225)
- Modify: `app/models/controllo_adozioni/passaggio_anno.rb` (`SQL_CLASSIFICA`:116, exists su new_adozioni/new_scuole; `anno` = `Miur.anno_corrente`)
- Modify: `app/models/controllo_adozioni/dashboard.rb` (`sql_righe`:95)
- Test: `test/controllers/controllo_adozioni_controller_test.rb` + eventuali unit test del namespace

Stessa procedura del Task 10. `NewScuola.maximum(:anno_scolastico)` → `Miur.anno_corrente`. Le fixture: se ancora `new_adozioni.yml`/`new_scuole.yml` (viste ponte), migra i test toccati alle fixture `miur/*` e al filtro anno.

**Commit** `refactor(miur): ControlloAdozioni::{Panoramica,PassaggioAnno,Dashboard} su miur_*`

---

## Task 12: Migra controller, jobs, Scuola, Classe, viste ERB

**Files:**
- Modify: `app/controllers/controllo_adozioni_controller.rb` (`libri_per_classe`:100)
- Modify: `app/controllers/controllo_adozioni/promozioni_controller.rb` (NewScuola/new_adozioni refs, `codice_nuovo_suggerito`:67)
- Modify: `app/jobs/aggiorna_cambi_codice_job.rb`, `app/jobs/aggiungi_scuole_nuove_job.rb`, `app/jobs/promuovi_scuole_promuovibili_job.rb`
- Modify: `app/models/scuola.rb` (`promuovibile?`:200, `roster_new_adozioni`:365 → rinomina `roster_miur`)
- Modify: `app/models/classe.rb` (refs NewAdozione/NewScuola/ImportAdozione:146-148 — `import_adozioni` di Classe passa a `Miur::Adozione.per_anno(...)`)
- Modify: `app/views/controllo_adozioni/show.html.erb`, `_cambi_codice.html.erb`, `_riga.html.erb`, `app/views/stats/_stat.html.erb` (refs new_adozioni nei testi/query)
- Test: `test/controllers/controllo_adozioni_controller_test.rb`, `test/jobs/aggiungi_scuole_nuove_job_test.rb`

Stessa procedura. Attenzione a `Scuola#promuovibile?` e ai job: replicano la stessa regola (debito noto, si consolida in Fase 2 — QUI solo rename tabelle/modelli, non refactoring).

**Gate:** suite completa verde. **Commit** `refactor(miur): controller/jobs/Scuola/Classe del controllo adozioni su miur_*`

---

## Task 13: Anteprima → anni al posto delle fonti

**Files:**
- Modify: `app/models/controllo_adozioni/anteprima.rb`
- Modify: `app/views/controllo_adozioni/_riga.html.erb` (i due link anteprima)
- Test: `test/models/controllo_adozioni/anteprima_test.rb`

La distinzione `fonte: new|import` diventa `anno:` — un solo ramo di codice su `Miur::Adozione.per_anno(anno)` + `Miur::Scuola`/`ImportScuola` per l'intestazione (per gli anni senza snapshot miur_scuole l'intestazione viene dall'anagrafe `ImportScuola` come oggi per fonte import). Param route: `?anno=202627` (default `Miur.anno_corrente`). TDD: riscrivi i test per i due anni, poi implementa. I test che usavano `ImportAdozione.import([...])` passano a `Miur::Adozione` (niente più workaround per l'override editore).

**Commit** `refactor(miur): Anteprima parametrizzata per anno su miur_adozioni`

---

## Task 14: Migra Stats + MCP tool

**Files:**
- Modify: `app/models/stats/new_adozioni_query.rb` → rinomina in `app/models/stats/miur_adozioni_query.rb` (classe `Stats::MiurAdozioniQuery`; le query aggiungono il filtro anno esplicito, di default `Miur.anno_corrente`)
- Modify: `app/tools/mcp_tools/stats_new_adozioni.rb` (usa la classe rinominata; il nome tool MCP resta `stats_new_adozioni` per compatibilità client — solo l'implementazione cambia)
- Test: cerca test esistenti del tool/query e adegua

⚠️ Memoria progetto: quando cambi un tool MCP va aggiornato anche scagnozz-cli (MCP + Cobra) e le 2 copie di SKILL.md — QUI il contratto del tool NON cambia (stesso nome, stessi parametri), quindi nessun aggiornamento CLI necessario. Se invece decidi di rinominare il tool, fallo presente all'utente PRIMA.

**Commit** `refactor(miur): Stats query su miur_adozioni (tool MCP invariato)`

---

## Task 15: Reconciler su tabella unica

**Files:**
- Modify: `app/models/adozione/reconciler.rb`
- Test: `test/models/adozione/reconciler_test.rb` (esiste? grep; altrimenti i test del job che lo usa)

La struct `Source` (righe 8-35) sparisce: unica tabella `miur_adozioni`, colonne minuscole, filtro `anno_scolastico = :anno` in OGNI subquery che oggi fa `FROM #{source.table}` (righe 96, 138, 163, 205, 231 — il filtro anno oggi è implicito nella scelta della tabella!). `stato` si deriva: `anno == Miur.anno_corrente ? "attiva" : "archiviata"`. `si.call` si unifica su `COALESCE(expr,'') ILIKE 'S%'` (funziona per entrambi i formati). L'hardcoded `"202627"` (riga 44) sparisce con `source`.

TDD: se non esistono test del Reconciler, scrivi PRIMA un test set-based minimo (account+scuola+2 righe miur_adozioni su 2 anni → `call` per anno corrente crea classe+adozioni attive; per anno passato crea archiviata) usando le fixture `miur/*`.

**Commit** `refactor(miur): Reconciler su miur_adozioni unica, stato derivato dall'anno`

---

## Task 16: Migra i consumer di import_adozioni

**Files:**
- Modify: `app/controllers/adozioni_comunicate_controller.rb` (righe 47, 211, 219-220) e `app/models/adozione_comunicata.rb` (righe 78, 239: `ImportAdozione.mie_adozioni` e match per ISBN)
- Modify: `app/controllers/titoli_controller.rb`:10
- Modify: `app/controllers/agenda_controller.rb` (SQL raw su import_adozioni — grep)
- Modify: `app/models/scuole/stats.rb`:16-17
- Modify: `app/models/adozione.rb`:134 (helper lookup)
- Modify: `app/jobs/import_scuole_per_zona_job.rb` (righe 6, 114, 154, 165: qui l'anno hardcoded `"202526"` diventa il parametro esplicito `anno:` del job con default `"202526"` — la semantica non cambia, Fase 2 lo collega ad AnnoScolastico)
- Test: relativi

Le query passano a `Miur::Adozione.per_anno("202526")` con colonne minuscole. Lo scope `mie_adozioni` di ImportAdozione va ricreato su Miur::Adozione (leggi la definizione in `import_adozione.rb` e portala SENZA i metodi titleize). `import_scuole_per_zona_job` scrive ancora `import_adozione_id: ia.id` — ora è l'id della riga miur_adozioni (partizione 202526, id preservati dal backfill: semanticamente identico).

**Gate:** suite completa verde. **Commit** `refactor(miur): consumer storici (comunicate, titoli, agenda, zone) su miur_adozioni`

---

## Task 17: Rimozione ponte + modelli e task legacy

**Files:**
- Create: `db/migrate/XXXX_drop_bridge_views.rb` — `DROP VIEW new_adozioni, new_scuole, import_adozioni` e **ricrea `view_classi`** con la stessa definizione ma `FROM miur_adozioni WHERE anno_scolastico='202526'` e alias UPPER inline (view_classi è ancora usata da chips/classi controller — NON rimuoverla)
- Delete: `app/models/new_adozione.rb`, `app/models/new_adozione/import.rb`, `app/models/new_scuola.rb`, `app/models/import_adozione.rb`, `app/models/old_adozione.rb`
- Delete: `test/fixtures/new_adozioni.yml`, `test/fixtures/new_scuole.yml` (se non già sostituite)
- Modify: `lib/tasks/import.rake` — rimuovi i task: `new_adozioni`, `new_scuole`, `cambia_religione`, `cambia_superiori` (→ se serve ancora, portalo in miur.rake su Miur::Adozione), `miur_adozioni`, `miur_scuole`, `splitta_adozioni`, `import_2024`, e le costanti staging orfane
- Modify: `lib/tasks/database_update.rake`, `lib/tasks/cache.rake` — rimuovi i task che toccano `old_adozioni`/`OldAdozione`
- Delete: `test/tasks/import_new_adozioni_test.rb` (testava il task rimosso; il failure Mocha pre-esistente sparisce con lui — se vuoi coprire `miur:importa_adozioni` scrivi l'equivalente `test/tasks/miur_importa_adozioni_test.rb` ricalcandone la struttura)

**Step 1:** Prima di cancellare ogni modello: `grep -rn 'NomeModello' app/ lib/ test/ config/` deve tornare vuoto (o solo il file stesso).

**Step 2:** Migration + `db:migrate` + **GATE FINALE: suite completa**. Expected: verde.

**Step 3:** Smoke test funzionale in dev:

```bash
docker exec prova-app-1 bin/rails miur:importa_adozioni   # swap partizione ok
docker exec prova-app-1 bin/rails runner 'ControlloAdozioni::Rebuild.run!'
```
Poi a occhio in browser: dashboard controllo_adozioni, una show scuola, un'anteprima per entrambi gli anni.

**Step 4: Commit** `refactor(miur)!: Rimosse viste ponte, modelli e task legacy delle swing tables`

---

## Note per il deploy in produzione (fuori scope del piano, da concordare)

1. Mettere in pausa il cron `adozioni_scraper` (già fatto in dev; in prod: commentare in `config/sidekiq.yml` o disabilitare via UI sidekiq-cron) PRIMA del deploy.
2. Il deploy porta tutte le migration: la finestra critica è il backfill (`miur:backfill`, ~10M righe, minuti) da lanciare A MANO dopo il deploy delle prime migration e PRIMA della migration del Task 9 — valutare se spezzare in due deploy (Task 1-8, poi 9-17).
3. Riattivare il cron a fine verifica.

## Fuori scope (fasi successive del design)

- `AnnoScolastico` value object, consolidamento `Classificazione`, Reconciler unico motore (Fase 2)
- UI stato-centrica + barra stato dati (Fase 3) — `miur_import_runs` è già pronta a alimentarla
- `import_scuole` → anagrafe durevole (Fase 4)
