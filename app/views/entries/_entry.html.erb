<%# locals: (entry:, draggable: true, as: nil) -%>
<% if entry.entryable %>
  <% if as.to_s == "storico_row" %>
    <%# Riga unificata dello storico delle schede (vista tabella di _closed_list) %>
    <%= render "entries/table/row", entry: entry %>
  <% elsif entry.entryable_type == "Appunto" %>
    <% if as.to_s == "row" %>
      <%= render partial: "appunti/table/row", locals: { appunto: entry.entryable, entry: entry } %>
    <% else %>
      <%= render partial: "appunti/appunto", locals: { appunto: entry.entryable, entry: entry, draggable: draggable } %>
    <% end %>
  <% elsif entry.entryable_type == "Documento" %>
    <% if as.to_s == "row" %>
      <%= render partial: "documenti/table/row", locals: { documento: entry.entryable, entry: entry } %>
    <% else %>
      <%= render partial: "documenti/documento", locals: { documento: entry.entryable, entry: entry, draggable: draggable } %>
    <% end %>
  <% elsif entry.entryable_type == "Tappa" %>
    <%= render partial: "tappe/tappa", locals: { tappa: entry.entryable, entry: entry, draggable: draggable } %>
  <% end %>
<% end %>
