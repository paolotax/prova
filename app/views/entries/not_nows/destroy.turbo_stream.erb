<%# Entry was resumed %>
<%= turbo_stream.remove dom_id(@entry) %>
<%= turbo_stream.prepend "awaiting_triage_entries" do %>
  <%= render partial: "entries/entry", locals: { entry: @entry } %>
<% end %>

<%= render "entries/replace_entryable_container" %>
