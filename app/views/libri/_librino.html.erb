<%= tag.div class: "flex flex-col mmmax-w-[300px]" do %>
    <%= tag.p libro.titolo, class: "text-wrap text-sm" %>

    <%= tag.div class: "flex flex-row justify-between items-center" do %>
        <%= tag.div libro.codice_isbn, class: "text-xs font-semibold text-gray-600" %>
        <%= tag.div number_to_currency(libro.prezzo), class: "text-xs font-semibold text-red-500" %>
        
    <% end %>
<% end %>