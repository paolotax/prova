<% @page_title = "Libri" %>
<% @body_class = "contained-scrolling" %>

<% content_for :head do %>
  <%= combobox_style_tag "data-turbo-track": "reload" %>
<% end %>

<% content_for :header do %>
  <div class="header__actions header__actions--start">
    <%= link_to new_import_path(type: :libri), class: "btn", data: { controller: "tooltip" } do %>
      <%= icon_tag "arrow-down-tray" %>
      <span class="for-screen-reader">Importa Libri</span>
    <% end %>
  </div>

  <div class="header__action--mobile-title">
    <%= link_to libri_path(format: :xlsx, params: request.query_parameters), class: "btn",
        data: { controller: "link-modifier tooltip", link_modifier_target: "link" } do %>
      <%= icon_tag "table-cells" %>
      <span class="for-screen-reader">Esporta in excel</span>
    <% end %>
  </div>

  <h1 class="header__title divider divider--fade full-width">
    <span class="overflow-ellipsis">Libri</span>
    <%= render "shared/vista_toggle", url: libri_path, vista: @vista %>
  </h1>

  <div class="header__actions header__actions--end">
    <%= link_to new_libro_path, class: "btn btn--link", data: { controller: "tooltip" } do %>
      <%= icon_tag "add" %>
      <span class="for-screen-reader">Nuovo libro</span>
    <% end %>
  </div>
<% end %>

<% if hotwire_native_app? %>
  <%= link_to "Nuovo Libro", new_libro_path, data: { controller: "bridge--button", bridge_title: "Nuovo" }, class: "hidden" %>
  <%= render "shared/native_search_filter",
      url: libri_path,
      placeholder: "Cerca..." %>
<% end %>

<%= render "filters/settings",
      filter_url: libri_path,
      filter_type: "libro_filter",
      user_filtering: @user_filtering,
      no_filtering_url: libri_path %>

<%= turbo_frame_tag :search_results do %>
  <% if @page.used? %>
    <p class="txt-small txt-subtle margin-block-end-half">
      <%= @total_count %> <%= @total_count == 1 ? "libro trovato" : "libri trovati" %>
    </p>

    <% if @vista == "tabella" %>
      <%= tag.div class: "flex flex-column", data: { controller: "bulk-actions" } do %>
        <%= render "libri/bulk_bar/bar" %>

        <%= tag.div class: "data-table data-table--scroller", style: Libro::Columns.style(@columns) do %>
          <%= render "shared/data_table/header", columns: @columns, sort: @sort, registry: Libro::Columns, url: libri_path %>

          <%= with_automatic_pagination :libri, @page,
              id: "libri-lista",
              data: { controller: "navigable-list",
                  navigable_list_auto_select_value: false,
                  navigable_list_actionable_items_value: true,
                  navigable_list_prevent_handled_keys_value: true,
                  action: "keydown->navigable-list#navigate" } do %>
            <% @page.records.each do |libro| %>
              <%= render "libri/table/row", libro: libro, columns: @columns %>
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    <% else %>
      <div class="cards cards--grid" data-controller="bulk-actions">
        <%= with_automatic_pagination :libri, @page,
            id: "libri-lista",
            class: "cards__list",
            data: { controller: "navigable-list",
                navigable_list_auto_select_value: false,
                navigable_list_actionable_items_value: true,
                navigable_list_prevent_handled_keys_value: true,
                action: "keydown->navigable-list#navigate keydown.space->navigable-list#toggleCurrentItem:prevent" } do %>
          <%= render "libri/display/previews", libri: @page.records %>
        <% end %>
        <%= render "libri/bulk_bar/bar" %>
      </div>
    <% end %>
  <% else %>
    <%= blank_slate_for(:libri, filtering: @user_filtering) %>
  <% end %>
<% end %>
