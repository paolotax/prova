<% @page_title = "Libri" %>
<% @body_class = "contained-scrolling" %>

<% content_for :head do %>
  <%#= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= combobox_style_tag "data-turbo-track": "reload" %>
<% end %>

<% content_for :header do %>
  <div class="header__actions header__actions--start">
    <%= link_to libri_path(format: :xlsx, params: request.query_parameters), class: "btn",
        data: { controller: "link-modifier", link_modifier_target: "link" } do %>
      <%= icon_tag "document" %>
      <span class="for-screen-reader">Esporta in excel</span>
    <% end %>
    <%= link_to new_libri_importer_path, class: "btn" do %>
      <%= icon_tag "document" %>
      <span class="for-screen-reader">Importa Libri</span>
    <% end %>
  </div>

  <h1 class="header__title divider divider--fade full-width">
    <span class="overflow-ellipsis">Libri</span>
  </h1>

  <div class="header__actions header__actions--end">
    <%= link_to new_libro_path, class: "btn" do %>
      <%= icon_tag "add" %>
      <span class="for-screen-reader">Nuovo appunto</span>
    <% end %>
  </div>
<% end %>

<% if hotwire_native_app? %>
	<%= link_to "Nuovo Libro", new_libro_path, data: { controller: "bridge--button", bridge_title: "Nuovo" }, class: "hidden" %>
    <%= render "shared/native_search_filter",
        url: libri_path,
        placeholder: "Cerca..." %>
<% end %>

<%= render "filters/settings",
      filter_url: libri_path,
      filter_type: "libro",
      user_filtering: @user_filtering,
      no_filtering_url: libri_path %>

<%= turbo_frame_tag :search_results do %>

    <%= tag.div class: "flex flex-col xl:pr-[15rem]", data: { controller: "bulk-actions" } do %>

        <%#= render "shared/active_filters", collection: @libri %>
        <%#= render "libri/bulk_actions/form" %>

        <%= tag.div id: "libri-lista", class: "grid grid-cols-1 lg:grid-cols-2 gap-half" do %>
            <p class="only:block hidden py-32 px-4 text-center col-span-full">Whoops! Non ci sono libri </p>
            
            <%= render @page.records %>

            <% unless @page.last? %>
                <%= turbo_frame_tag "load_more",
                            src: libri_path(request.query_parameters.merge(page: @page.next_param, format: :turbo_stream)),
                            loading: :lazy do %>
                    <%= render 'shared/lazy_loading' %>
                <% end %>
            <% end %>
        <% end %>

    <% end %>

    <aside class="fixed inset-y-0 right-0 hidden w-72 overflow-y-auto pt-[184px] lg:px-8 xl:block">
        <!-- Secondary column (hidden on smaller screens) -->
        <%#= render partial: "filter_form", locals: { reload: true } %>
    </aside>

<% end %>

<%= render "libri/bulk_actions/form" %>








