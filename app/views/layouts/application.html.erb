<!DOCTYPE html>

<html>
	<head>
		<title>Prova</title>
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>
		
		<%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
		<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
		
		<%= javascript_importmap_tags %>
	</head>


	<body class="antialiased">
		
		<%= render partial: "import_scuole/dialog" %>
		
		<!--sarebbe bello ma non funzionanano i link nella prima pagina -- CORREGGERE-->
		<%#= render partial: "layouts/back_to_top" %>

		<div class="h-full" >
				
			<%= render "layouts/slide_over" %>

			<div class="hidden lg:fixed lg:inset-y-0 lg:z-40 lg:flex lg:w-72 lg:flex-col">
				<%= render "layouts/sidebar" %>
			</div>

			<div class="lg:pl-72" data-controller="queryopt">

				<%= render "layouts/main_menu" %>
		
				<main class="px-2 py-4 sm:px-6 lg:px-8">		
						
						<!-- Your content -->
						<%= yield %>
					
				</main>
 
			</div>  
		</div>
	
	</body>

</html>
