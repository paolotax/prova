<%= turbo_frame_tag :closed_column do %>
  <%= with_automatic_pagination :closed_column, @page,
      class: "cards__list",
      data: { drag_drop_item_container: true } do %>
    <%= render partial: "entries/entry", collection: @page.records, as: :entry %>
  <% end %>
<% end %>
