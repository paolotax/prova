<% @page_title = @scontabile ? "Modifica sconto per #{@scontabile.respond_to?(:denominazione) ? @scontabile.denominazione : @scontabile}" : "Modifica sconto" %>

<% content_for :header do %>
  <div class="header__actions header__actions--start">
    <%= back_link_to "Sconti", @scontabile ? polymorphic_path([ @scontabile, :sconti ]) : sconti_path %>
  </div>

  <h1 class="header__title"><%= @page_title %></h1>

  <div class="header__actions header__actions--end"></div>
<% end %>

<section class="panel shadow center">
  <%= render "form", sconto: @sconto %>
</section>
