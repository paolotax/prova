<%# locals: ( reload: false ) -%>

<%= component 'tax_filter_form', base_url: appunti_path, reload: reload,
    
    fields: [ 
        {
            field: :search,
            label: "Cerca",
            collection: nil,
            type: :text,
            placeholder: "cerca..."
        }, {
            field: :statuses,
            label: "Status",
            collection: Appunto::STATO_APPUNTI.map(&:itself),
            type: :checkbox_group,
            placeholder: "status..."
        }
    
    ] 
%>
