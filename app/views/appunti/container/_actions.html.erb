<%# locals: (appunto:) -%>

<%= link_to appunto_path(appunto, format: :pdf), class: "btn", data: { controller: "tooltip", tooltip_content_value: "Riapri" } do %>
  <%= icon_tag "document" %>
  <span class="for-screen-reader">Stampa appunto</span>
<% end %>
