<%# locals: (appunto:) -%>

<% if appunto.drafted? %>
  <%= render "appunti/container/footer/create", appunto: appunto %>
<% else %>
  <%= render "appunti/container/footer/published", appunto: appunto %>
<% end %>
