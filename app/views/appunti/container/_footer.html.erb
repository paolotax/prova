<%# locals: (appunto:) -%>

<%= link_to edit_appunto_path(appunto), class: "btn" do %>
  <%= icon_tag "pencil" %>
  Modifica
<% end %>

<% unless appunto.closed? %>
  <%= button_to appunto_closure_path(appunto), method: :post, class: "btn btn--reversed" do %>
    <%= icon_tag "check" %>
    Completa
  <% end %>
<% end %>
