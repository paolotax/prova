<%= render "shared/turbo_dialog" do %>
  <div data-action="turbo:submit-end->dialog#submitEnd">
      <h1 class="font-bold text-4xl">Nuovo appunto</h1>
      <%= render "form", appunto: @appunto %>
  </div>
<% end %>
