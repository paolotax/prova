<%= turbo_stream.replace(dom_id(@column), partial: "columns/dashboard_column", method: :morph, locals: { column: @column, count: current_account.entries.published.active.in_column(@column).count }) %>
