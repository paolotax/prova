<%= form_with(model: [Current.user, personal_info],
      method: personal_info&.persisted? ? :patch : :post,
      multipart: true,
      data: { controller: "form upload-preview" }, class: "flex flex-column gap") do |form| %>
  <% if personal_info.errors.any? %>
    <div class="panel pad border-radius txt-negative fill-shade">
      <h3 class="font-weight-black"><%= pluralize(personal_info.errors.count, "errore", "errori") %> da correggere:</h3>
      <ul class="margin-block-start-half txt-small">
        <% personal_info.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Avatar -->
  <section class="panel pad border-radius shadow">
    <div class="avatar__form flex align-center gap">
      <div class="relative">
        <label class="avatar avatar--large cursor-pointer">
          <%= image_tag user_avatar_path(Current.user, v: Current.user.updated_at.to_i), alt: "Avatar", class: "avatar", size: 128, data: { upload_preview_target: "image" } %>
          <%= file_field_tag :avatar, class: "for-screen-reader", accept: "image/jpeg,image/png,image/gif,image/webp",
                data: { upload_preview_target: "input", action: "upload-preview#previewImage" } %>
        </label>
      </div>

      <% if Current.user.avatar.attached? %>
        <%= tag.button type: :submit, form: "avatar-delete-form",
              class: "btn txt-negative txt-small",
              data: { turbo_confirm: "Sei sicuro di voler rimuovere il tuo avatar?" } do %>
          <%= icon_tag "trash" %>
          <span class="hide-on-mobile">Elimina avatar</span>
        <% end %>
      <% end %>
    </div>
  </section>

  <!-- Dati personali -->
  <section class="panel pad border-radius shadow">
    <h2 class="txt-medium font-weight-black margin-block-end">Dati personali</h2>

    <div class="flex flex-column gap">
      <div class="form-grid">
        <div class="form-field">
          <%= form.label :nome, class: "form-label" %>
          <%= form.text_field :nome, class: "input", placeholder: "Mario" %>
        </div>

        <div class="form-field">
          <%= form.label :cognome, class: "form-label" %>
          <%= form.text_field :cognome, class: "input", placeholder: "Rossi" %>
        </div>
      </div>

      <div class="form-grid">
        <div class="form-field">
          <%= form.label :cellulare, class: "form-label" %>
          <%= form.telephone_field :cellulare, class: "input mono", placeholder: "+39 333 1234567" %>
        </div>

        <div class="form-field">
          <%= form.label :email_personale, "Email personale", class: "form-label" %>
          <%= form.email_field :email_personale, class: "input", placeholder: "mario.rossi@email.com" %>
        </div>
      </div>
    </div>
  </section>

  <!-- Preferenze -->
  <section class="panel pad border-radius shadow">
    <h2 class="txt-medium font-weight-black margin-block-end">Preferenze</h2>

    <div class="form-field">
      <%= form.label :navigator, "Navigatore preferito", class: "form-label" %>
      <%= form.select :navigator,
            options_for_select([
              ["Google Maps", "google_maps"],
              ["Apple Maps", "apple_maps"],
              ["Waze", "waze"]
            ], personal_info.navigator),
            { include_blank: "Seleziona..." },
            class: "input input--select" %>
    </div>
  </section>

  <div class="flex align-center justify-end gap">
    <%= link_to "Annulla", user_personal_info_path(Current.user), class: "btn" %>
    <%= form.submit personal_info.persisted? ? "Aggiorna" : "Salva", class: "btn btn--primary" %>
  </div>
<% end %>

<%= form_with url: user_avatar_path(Current.user), method: :delete, id: "avatar-delete-form" %>
