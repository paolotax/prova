<% @page_title = @scuola.denominazione %>
<% @header_class = "header--card" %>
<% @body_class = "contained-scrolling" %>
<% content_for :head, tag.meta(name: "turbo-cache-control", content: "no-cache") %>

<%= turbo_stream_from Current.account, "scuole" %>

<% content_for :header do %>
  <div class="header__actions header__actions--start">
    <% info = referrer_back_info || { label: "", path: scuole_path } %>
    <%= back_link_to info[:label], info[:path] %>
  </div>
<% end %>

<% if Current.scuole.where(id: @scuola.id).exists? %>
  <div class="full-width pad group">
    <%= render "scuole/container", scuola: @scuola %>
  </div>

  <% if @scuola.deletable? %>
    <footer class="txt-align-center full-width flex flex-column margin-block-start-double">
      <hr class="separator--horizontal full-width margin-block" style="--border-color: var(--color-ink-lighter)">
      <%= render "shared/container/delete", record: @scuola, path: scuola_path(@scuola), label: "questa scuola" %>
    </footer>
  <% end %>

<% else %>
  <div class="blank-slate blank-slate--empty" style="margin-block-start: 4rem;">
    <h3 class="txt-large font-weight-bold">Scuola non disponibile</h3>
    <p class="txt-small txt-subtle margin-block-end">
      Questa scuola non è più tra quelle a te assegnate.
    </p>
    <%= link_to scuole_path, class: "btn" do %>
      Torna alle scuole
    <% end %>
  </div>
<% end %>
