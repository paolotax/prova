<%= form_with(model: import_scuola, class: "contents") do |form| %>
  <% if import_scuola.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(import_scuola.errors.count, "error") %> prohibited this import_scuola from being saved:</h2>

      <ul>
        <% import_scuola.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
