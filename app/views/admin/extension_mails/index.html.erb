<h2>Invia estensione WhatsApp Web</h2>
<p class="txt-subtle margin-block-end-double">
  Seleziona gli utenti a cui inviare l'estensione Chrome WhatsApp Web → ScagnoZZ
</p>

<%= form_with url: admin_extension_mails_path, method: :post do |f| %>
  <table class="table table--compact admin-table--narrow margin-block-end">
    <thead>
      <tr>
        <th>
          <input type="checkbox" id="select-all" onchange="document.querySelectorAll('input[name=\\'user_ids[]\\']').forEach(cb => cb.checked = this.checked)">
        </th>
        <th>Nome</th>
        <th>Email</th>
        <th>Ruolo</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td>
            <%= check_box_tag "user_ids[]", user.id, false, id: "user_#{user.id}" %>
          </td>
          <td><%= user.name %></td>
          <td class="txt-subtle"><%= user.email %></td>
          <td><%= user.role %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= f.submit "Invia estensione", style: "padding: 0.5rem 1.5rem; background: oklch(var(--lch-red-dark)); color: var(--color-ink-inverted); border: none; border-radius: 0.5rem; cursor: pointer; font-size: 0.875rem;" %>
<% end %>
