<%= render "shared/turbo_dialog" do %>
    <div data-action="turbo:submit-end->dialog#submitEnd" class="w-full">

        <h2 class="font-bold text-2xl">Nuovo giro</h2>
        <%= render "form", giro: @giro %>
        
    </div>
<% end %>
