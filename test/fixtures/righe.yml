# == Schema Information
#
# Table name: righe
#
#  id                     :bigint           not null, primary key
#  iva_cents              :integer          default(0)
#  prezzo_cents           :integer          default(0)
#  prezzo_copertina_cents :integer          default(0)
#  quantita               :integer          default(1)
#  sconto                 :decimal(5, 2)    default(0.0)
#  status                 :integer
#  created_at             :datetime         not null
#  updated_at             :datetime         not null
#  libro_id               :bigint           not null
#
# Indexes
#
#  index_righe_on_libro_id  (libro_id)
#
# Foreign Keys
#
#  fk_rails_...  (libro_id => libri.id)
#
riga_documento_fizzy:
  libro: libro_fizzy
  quantita: 10
  prezzo_cents: 10000
  sconto: 0.0

riga_fattura_uno:
  libro: libro_fizzy
  quantita: 20
  prezzo_cents: 10000
  sconto: 0.0

riga_fattura_due:
  libro: libro_fizzy
  quantita: 15
  prezzo_cents: 10000
  sconto: 0.0

riga_ordine_figlio:
  libro: libro_fizzy
  quantita: 8
  prezzo_cents: 10000
  sconto: 0.0

riga_nota_credito:
  libro: libro_fizzy
  quantita: 3
  prezzo_cents: 10000
  sconto: 0.0

riga_documento_acme:
  libro: libro_acme
  quantita: 5
  prezzo_cents: 10000
  sconto: 0.0

# Riga per ddt_fornitore_fizzy (causale gestione_consegna: false), per verificare
# che Saldo#residui_consegne escluda il documento nonostante abbia righe reali.
riga_ddt_fornitore:
  libro: libro_fizzy
  quantita: 5
  prezzo_cents: 10000
  sconto: 0.0
