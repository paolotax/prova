---
phase: 08-form-views
plan: 03
type: execute
wave: 1
depends_on: ["08-02"]
files_modified:
  - app/assets/stylesheets/documenti.css
  - app/views/documenti/_form.html.erb
  - app/views/documento_righe/_documento_riga.html.erb
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "_form.html.erb contains no Tailwind utility classes"
    - "Inline edit fields (status, dates) have proper styling"
    - "Number inputs in riga partial have consistent styling"
  artifacts:
    - path: "app/assets/stylesheets/documenti.css"
      provides: "BEM classes for inline fields and form inputs"
      contains: ".documento-form__inline-field"
    - path: "app/views/documenti/_form.html.erb"
      provides: "Form with zero Tailwind utility classes"
      pattern: "documento-form__inline"
    - path: "app/views/documento_righe/_documento_riga.html.erb"
      provides: "Riga with BEM input classes"
      pattern: "documento-form__input"
  key_links:
    - from: "inline_fields block in _form.html.erb"
      to: "documenti.css"
      via: "BEM class references"
      pattern: "documento-form__inline"
    - from: "number_field inputs in _documento_riga.html.erb"
      to: "documenti.css"
      via: "BEM class references"
      pattern: "documento-form__input"
---

<objective>
Close verification gaps: migrate remaining Tailwind classes in inline_fields block and number_field inputs to BEM classes.

Purpose: Achieve ROADMAP success criteria requiring "_form.html.erb contains no Tailwind utility classes".

Output: Both form partials with zero Tailwind utility classes (only preserving functional classes: cb-tax, cb-tax-fancy, handle, group-[.inline-edit]:).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-form-views/08-VERIFICATION.md
@.planning/phases/08-form-views/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline field and input BEM classes to documenti.css</name>
  <files>app/assets/stylesheets/documenti.css</files>
  <action>
Add new BEM classes inside the Phase 8 section (after `.documento-form__delete-button` block, before "Dark mode overrides" comment at line 915).

**Inline fields container (for inline_fields block):**

```css
/* Inline fields container (status/payment toggles) */
.documento-form__inline-container {
  align-items: flex-start;
  color: var(--color-ink);
  display: flex;
  flex-direction: column;
  gap: 0.5rem;

  @media (min-width: 640px) {
    margin-inline-end: 1rem;
  }
}

/* Inline field group (label + input pair) */
.documento-form__inline-field {
  display: flex;
  flex-direction: column;
}

/* Inline select/date inputs */
.documento-form__inline-select,
.documento-form__inline-date {
  background-color: var(--color-canvas);
  border: 1px solid var(--color-ink-light);
  border-radius: 0.375rem;
  box-shadow: var(--shadow);
  outline: none;
  padding: 0.5rem 0.75rem;
}

.documento-form__inline-select:focus,
.documento-form__inline-date:focus {
  border-color: oklch(var(--lch-blue-medium));
  box-shadow: 0 0 0 1px oklch(var(--lch-blue-medium));
}
```

**Number input styling (for quantita/prezzo/sconto):**

```css
/* Number input base styling */
.documento-form__input-number {
  background-color: var(--color-canvas);
  border: none;
  border-radius: 0.375rem;
  box-shadow: inset 0 0 0 1px var(--color-ink-light);
  color: var(--color-ink);
  inline-size: 100%;
  padding: 0.5rem;
  text-align: end;

  /* Hide spinner buttons */
  appearance: textfield;
  -moz-appearance: textfield;

  &::-webkit-outer-spin-button,
  &::-webkit-inner-spin-button {
    appearance: none;
    -webkit-appearance: none;
    margin: 0;
  }

  &:focus {
    box-shadow: inset 0 0 0 2px oklch(var(--lch-blue-medium));
    outline: none;
  }

  &::placeholder {
    color: var(--color-ink-medium);
  }
}
```

IMPORTANT:
- Insert AFTER the `.documento-form__delete-button` block (around line 913)
- Insert BEFORE the "Dark mode overrides" comment (line 915)
- Keep all existing classes unchanged
  </action>
  <verify>
Confirm new classes exist:
```bash
grep -c "documento-form__inline" /home/paolotax/rails_2023/prova/app/assets/stylesheets/documenti.css
```
Should return 4+ (container, field, select, date).

```bash
grep -c "documento-form__input-number" /home/paolotax/rails_2023/prova/app/assets/stylesheets/documenti.css
```
Should return 1+.
  </verify>
  <done>
documenti.css contains:
- .documento-form__inline-container (flex column with gap)
- .documento-form__inline-field (label+input pair)
- .documento-form__inline-select, .documento-form__inline-date (input styling)
- .documento-form__input-number (right-aligned number with hidden spinners)
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate inline_fields block in _form.html.erb</name>
  <files>app/views/documenti/_form.html.erb</files>
  <action>
Replace Tailwind classes in the inline_fields block (lines 33-52) with BEM classes.

**1. Outer container (line 34):**
```erb
# FROM:
<%= tag.div class: "sm:mr-4 flex flex-col space-x-2 items-start text-black" do %>

# TO:
<%= tag.div class: "documento-form__inline-container" do %>
```

**2. Field groups (lines 35, 39, 43, 47):**
```erb
# FROM (4 instances):
<%= tag.div class: "flex flex-col" do %>

# TO (4 instances):
<%= tag.div class: "documento-form__inline-field" do %>
```

**3. Select inputs (lines 37, 45):**
```erb
# FROM:
<%= form.select :status, ..., {}, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2" %>
<%= form.select :tipo_pagamento, ..., {}, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2" %>

# TO:
<%= form.select :status, ..., {}, class: "documento-form__inline-select" %>
<%= form.select :tipo_pagamento, ..., {}, class: "documento-form__inline-select" %>
```

**4. Date inputs (lines 41, 49):**
```erb
# FROM:
<%= form.date_field :consegnato_il, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2" %>
<%= form.date_field :pagato_il, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2" %>

# TO:
<%= form.date_field :consegnato_il, class: "documento-form__inline-date" %>
<%= form.date_field :pagato_il, class: "documento-form__inline-date" %>
```

IMPORTANT PRESERVE:
- All form.label elements unchanged
- All data-* attributes unchanged
- The `render "shared/inline_fields"` call unchanged
- All Ruby code (options_for_select, etc.) unchanged
- The closing `<% end %>` tags unchanged
  </action>
  <verify>
No Tailwind utility classes remain in _form.html.erb:
```bash
grep -E "(flex-col|shadow|rounded-md|border-gray|px-3|py-2|sm:mr-4|space-x|items-start|text-black)" /home/paolotax/rails_2023/prova/app/views/documenti/_form.html.erb | wc -l
```
Should return 0.

BEM classes present:
```bash
grep -c "documento-form__inline" /home/paolotax/rails_2023/prova/app/views/documenti/_form.html.erb
```
Should return 6+ (1 container, 4 fields, 4 inputs).
  </verify>
  <done>
_form.html.erb inline_fields block uses:
- documento-form__inline-container (outer wrapper)
- documento-form__inline-field (4 instances for field groups)
- documento-form__inline-select (2 instances for selects)
- documento-form__inline-date (2 instances for date fields)
- Zero Tailwind utility classes remain
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate number inputs in _documento_riga.html.erb</name>
  <files>app/views/documento_righe/_documento_riga.html.erb</files>
  <action>
Replace Tailwind classes on number_field inputs (lines 37-64) with BEM class.

**1. Quantita input (lines 37-42):**
```erb
# FROM:
<%= form_riga.number_field :quantita,
        data: { tax_combobox_libro_target: "quantita" },
        class: [
            "text-right [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none": true,
            "w-full rounded-md border-0 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-gray-600": true
            ] %>

# TO:
<%= form_riga.number_field :quantita,
        data: { tax_combobox_libro_target: "quantita" },
        class: "documento-form__input-number" %>
```

**2. Prezzo input (lines 47-53):**
```erb
# FROM:
<%= form_riga.number_field :prezzo,
        data: { tax_combobox_libro_target: "prezzo" },
        step: 0.01,
        class: [
            "text-right [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none": true,
            "w-full rounded-md border-0 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-gray-600": true
        ] %>

# TO:
<%= form_riga.number_field :prezzo,
        data: { tax_combobox_libro_target: "prezzo" },
        step: 0.01,
        class: "documento-form__input-number" %>
```

**3. Sconto input (lines 58-64):**
```erb
# FROM:
<%= form_riga.number_field :sconto,
        data: { tax_combobox_libro_target: "sconto" },
        step: 0.01,
        class: [
            "text-right [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none": true,
            "w-full rounded-md border-0 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-gray-600": true
        ] %>

# TO:
<%= form_riga.number_field :sconto,
        data: { tax_combobox_libro_target: "sconto" },
        step: 0.01,
        class: "documento-form__input-number" %>
```

IMPORTANT PRESERVE:
- All data-* attributes unchanged
- step: 0.01 on prezzo and sconto unchanged
- All other lines in the file unchanged
  </action>
  <verify>
No Tailwind utility classes remain on number inputs:
```bash
grep -E "(text-right|appearance:textfield|ring-1|ring-gray|text-gray-900|shadow-sm|border-0)" /home/paolotax/rails_2023/prova/app/views/documento_righe/_documento_riga.html.erb | wc -l
```
Should return 0.

BEM class present:
```bash
grep -c "documento-form__input-number" /home/paolotax/rails_2023/prova/app/views/documento_righe/_documento_riga.html.erb
```
Should return 3.
  </verify>
  <done>
_documento_riga.html.erb number inputs use:
- documento-form__input-number (3 instances for quantita, prezzo, sconto)
- Zero Tailwind utility classes on inputs
- Preserves data-* attributes and step values
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. CSS file contains new inline field and input classes:
```bash
grep -c "documento-form__inline" /home/paolotax/rails_2023/prova/app/assets/stylesheets/documenti.css && grep -c "documento-form__input-number" /home/paolotax/rails_2023/prova/app/assets/stylesheets/documenti.css
```
Should return 4+ and 1+.

2. _form.html.erb has zero Tailwind utilities:
```bash
grep -E "(flex-col|shadow|rounded-md|border-gray|px-3|py-2|sm:mr-4|space-x|items-start|text-black)" /home/paolotax/rails_2023/prova/app/views/documenti/_form.html.erb | wc -l
```
Should return 0.

3. _documento_riga.html.erb has zero Tailwind utilities on inputs:
```bash
grep -E "(text-right|appearance:textfield|ring-1|ring-gray|text-gray-900|shadow-sm|border-0)" /home/paolotax/rails_2023/prova/app/views/documento_righe/_documento_riga.html.erb | wc -l
```
Should return 0.

4. Rails loads without errors:
```bash
docker exec -it prova-app-1 bin/rails runner "puts 'OK'"
```
</verification>

<success_criteria>
1. `_form.html.erb` contains no Tailwind utility classes (gap closed)
2. `_documento_riga.html.erb` contains no Tailwind utility classes on inputs (gap closed)
3. New BEM classes provide equivalent styling (shadow, border, focus states)
4. All functional classes preserved (cb-tax, cb-tax-fancy, handle)
5. All data-* attributes preserved for Stimulus controllers
6. CSS syntax valid (Rails loads without errors)
</success_criteria>

<output>
After completion, create `.planning/phases/08-form-views/08-03-SUMMARY.md`
</output>
