---
phase: 08-form-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/stylesheets/documenti.css
autonomous: true

must_haves:
  truths:
    - "Form sections visually distinguish header, clientable, and righe areas"
    - "Riga rows display with proper grid alignment for libro/quantity/price/delete fields"
    - "Error messages are styled with red background and text"
    - "Submit button has blue background with hover state"
  artifacts:
    - path: "app/assets/stylesheets/documenti.css"
      provides: "documento-form BEM block with section, fieldset, riga elements"
      contains: ".documento-form"
  key_links:
    - from: ".documento-form__section"
      to: "grid layout"
      via: "display: grid with responsive columns"
      pattern: "grid-template-columns.*repeat\\(4|8"
    - from: ".documento-form__riga"
      to: "grid layout"
      via: "align-items: end for field alignment"
      pattern: "align-items.*end"
---

<objective>
Add CSS infrastructure for the documento form component to documenti.css.

Purpose: Provide BEM classes for form sections, fieldsets, riga rows, and actions so the ERB migration (Plan 02) can reference semantic classes instead of Tailwind utilities.

Output: Updated documenti.css with .documento-form block and all required elements/modifiers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-form-views/08-RESEARCH.md

# Prior phase summaries
@.planning/phases/04-foundation/04-01-SUMMARY.md
@.planning/phases/07-detail-view/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add documento-form BEM block to documenti.css</name>
  <files>app/assets/stylesheets/documenti.css</files>
  <action>
Add form styles inside the Phase 8 placeholder section (between "Form Views - Phase 8" and "Dark mode overrides").

**Block and container elements:**

```css
/* Form container - uses contents for grid inheritance */
.documento-form {
  display: contents;
}

/* Error display */
.documento-form__errors {
  background-color: oklch(var(--lch-red-lightest));
  border-radius: 0.5rem;
  color: oklch(var(--lch-red-medium));
  font-weight: 500;
  margin-block-start: 0.75rem;
  padding: 0.5rem 0.75rem;
}
```

**Section elements (header, clientable, righe):**

```css
/* Form sections with responsive grid */
.documento-form__section {
  background-color: var(--color-ink-lighter);
  border: 1px solid var(--color-ink-light);
  border-radius: 0.5rem;
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(4, 1fr);
  margin-block: 1rem;
  padding: 1.25rem 1.5rem;

  @media (min-width: 640px) {
    grid-template-columns: repeat(8, 1fr);
  }
}

.documento-form__section--header {
  /* Uses default grid styling */
}

.documento-form__section--clientable {
  display: flex;
  flex-direction: column;
  margin-block-end: 1rem;
}

.documento-form__section--righe {
  background-color: transparent;
  border: none;
  display: block;
  margin-block: 0;
  padding: 0;
}
```

**Fieldset column positioning:**

```css
/* Fieldset containers */
.documento-form__fieldset {
  display: flex;
  flex-direction: column;
  margin-block: 1.25rem;
}

.documento-form__fieldset--causale {
  grid-column: span 4;
}

.documento-form__fieldset--numero {
  grid-column: span 2;
}

.documento-form__fieldset--data {
  grid-column: span 2;
}

.documento-form__fieldset--small {
  margin-block: 0.5rem;
}
```

**Riga row with responsive grid:**

```css
/* Single riga row */
.documento-form__riga {
  align-items: end;
  background-color: var(--color-canvas);
  border: 1px solid var(--color-ink-light);
  border-radius: 0.5rem;
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(4, 1fr);
  margin-block-end: 0.5rem;
  padding: 0.625rem 1rem;

  @media (min-width: 640px) {
    grid-template-columns: repeat(8, 1fr);
  }
}

.documento-form__riga-handle {
  cursor: move;
  flex-shrink: 0;
  padding-inline-end: 1rem;
}

.documento-form__riga-libro {
  align-items: center;
  display: flex;
  grid-column: span 4;
}

.documento-form__riga-field {
  align-items: center;
  display: flex;
  flex-direction: column;
  grid-column: span 1;
  justify-content: space-between;
}

.documento-form__riga-field--prezzo {
  @media (max-width: 639px) {
    grid-column-start: 2;
  }
}

.documento-form__riga-delete {
  align-items: center;
  display: flex;
  grid-column: span 1;
  justify-content: center;
}
```

**Action buttons:**

```css
/* Form actions container */
.documento-form__actions {
  align-items: flex-start;
  display: flex;
  margin-block-end: 2rem;
  padding-inline: 1rem;
}

/* Submit button */
.documento-form__submit {
  background-color: oklch(var(--lch-blue-dark));
  border-radius: 0.5rem;
  color: var(--color-ink-inverted);
  cursor: pointer;
  display: inline-block;
  font-weight: 500;
  padding: 0.75rem 1.25rem;
  transition: background-color 150ms ease;

  &:hover {
    background-color: oklch(var(--lch-blue-medium));
  }

  &:disabled {
    background-color: var(--color-ink-light);
    cursor: not-allowed;
    opacity: 0.5;
  }
}
```

**Delete button (circular red):**

```css
/* Delete button for riga rows */
.documento-form__delete-button {
  align-items: center;
  background-color: oklch(var(--lch-red-dark));
  block-size: 1.75rem;
  border-radius: 9999px;
  box-shadow: var(--shadow);
  color: var(--color-ink-inverted);
  display: flex;
  inline-size: 1.75rem;
  justify-content: center;
  margin-block-end: 0.375rem;
  margin-inline: 0.5rem;
  transition: background-color 150ms ease;

  &:hover {
    background-color: oklch(var(--lch-red-medium));
  }

  &:focus-visible {
    outline: 2px solid oklch(var(--lch-red-dark));
    outline-offset: 2px;
  }

  &:disabled {
    background-color: var(--color-ink-lighter);
    color: var(--color-ink-medium);
    cursor: not-allowed;
    opacity: 0.5;
  }
}
```

IMPORTANT:
- Place all styles INSIDE the existing `@layer components { }` block
- Insert between the "Form Views - Phase 8" comment and "Dark mode overrides" comment
- Use existing design tokens (--color-*, --lch-*, --shadow)
- Keep inline_edit and combobox classes unchanged (cb-tax, cb-tax-fancy remain Tailwind)
  </action>
  <verify>
Run in Docker container:
```bash
docker exec -it prova-app-1 bin/rails runner "puts 'CSS file valid'" && echo "Ruby environment OK"
```
Then verify the CSS file contains the new classes:
```bash
grep -c "documento-form" app/assets/stylesheets/documenti.css
```
Should return a count greater than 0.
  </verify>
  <done>
documenti.css contains .documento-form block with:
- __errors (red background/text)
- __section with --header, --clientable, --righe modifiers
- __fieldset with --causale, --numero, --data, --small modifiers
- __riga, __riga-handle, __riga-libro, __riga-field, __riga-delete
- __actions, __submit (blue with hover/disabled states)
- __delete-button (circular red with hover/disabled states)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify CSS syntax and browser loading</name>
  <files>app/assets/stylesheets/documenti.css</files>
  <action>
Verify the CSS file has no syntax errors by:

1. Check CSS file is valid (no unclosed braces, valid property names):
```bash
docker exec -it prova-app-1 bin/rails assets:precompile --dry-run
```

2. If rails assets:precompile is slow, alternatively just verify the file loads:
```bash
docker exec -it prova-app-1 bin/rails runner "require 'open-uri'; puts 'CSS loadable'"
```

3. Count total lines to ensure file grew appropriately (should be ~900+ lines after adding ~150 lines of form styles):
```bash
wc -l app/assets/stylesheets/documenti.css
```

Do NOT modify files in this task - verification only.
  </action>
  <verify>
- wc -l returns approximately 880-920 lines (was 743, adding ~150)
- No CSS syntax errors reported
- Rails environment loads without errors
  </verify>
  <done>
CSS file valid and loadable, line count confirms form styles were added.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. documenti.css contains `.documento-form` block (grep confirms)
2. CSS file has no syntax errors (Rails loads without errors)
3. File size increased appropriately (~150 new lines)
</verification>

<success_criteria>
1. `.documento-form` BEM block exists in documenti.css with all documented elements
2. Section modifiers (--header, --clientable, --righe) provide distinct layouts
3. Riga grid aligns libro/quantity/price/delete fields
4. Submit button has blue background with hover state
5. Delete button has circular red style with hover/disabled states
6. No CSS syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-form-views/08-01-SUMMARY.md`
</output>
