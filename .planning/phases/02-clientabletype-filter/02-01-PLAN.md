---
phase: 02-clientabletype-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/filters/documento/fields.rb
autonomous: true

must_haves:
  truths:
    - "Filter object has clientable_type accessor"
    - "clientable_type returns nil for empty/blank values"
    - "clientable_type appears in as_params when set"
  artifacts:
    - path: "app/models/filters/documento/fields.rb"
      provides: "clientable_type store_accessor with getter/setter"
      contains: "clientable_type"
  key_links:
    - from: "app/models/filters/documento/fields.rb"
      to: "app/models/filters/documento.rb"
      via: "include Documento::Fields"
      pattern: "store_accessor.*clientable_type"
---

<objective>
Add clientable_type field to Filters::Documento::Fields

Purpose: Enable storing clientable_type filter value in the filter object
Output: Updated Fields module with clientable_type accessor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/models/filters/documento/fields.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clientable_type to Fields module</name>
  <files>app/models/filters/documento/fields.rb</files>
  <action>
  Modify app/models/filters/documento/fields.rb to add clientable_type support:

  1. Add :clientable_type to PERMITTED_PARAMS array (as a scalar, not array)

  2. Add :clientable_type to store_accessor list (after :pagati)

  3. Add getter method for clientable_type (follows anno pattern for scalar fields):
     ```ruby
     def clientable_type
       super.presence
     end
     ```

  4. Add clientable_type to as_params hash:
     ```ruby
     params[:clientable_type] = clientable_type
     ```

  Note: clientable_type is a SCALAR field (like anno), NOT an array field (like causali).
  Valid values will be "Cliente" or "ImportScuola".
  </action>
  <verify>
  Run in Rails console inside Docker:
  ```bash
  docker exec -it prova-app-1 bin/rails runner "
    f = Filters::Documento.new
    puts 'accessor exists: ' + f.respond_to?(:clientable_type).to_s
    f.clientable_type = 'Cliente'
    puts 'can set value: ' + (f.clientable_type == 'Cliente').to_s
    f.clientable_type = ''
    puts 'blank returns nil: ' + f.clientable_type.nil?.to_s
    f.clientable_type = 'ImportScuola'
    puts 'in as_params: ' + f.as_params[:clientable_type].to_s
  "
  ```
  All checks should print true/ImportScuola.
  </verify>
  <done>
  - PERMITTED_PARAMS includes :clientable_type
  - store_accessor includes :clientable_type
  - Getter returns nil for blank values
  - as_params includes clientable_type when set
  </done>
</task>

</tasks>

<verification>
- Fields module loads without syntax errors
- Filter object responds to clientable_type
- Value persistence works correctly
</verification>

<success_criteria>
Filters::Documento.new.respond_to?(:clientable_type) returns true
</success_criteria>

<output>
After completion, create `.planning/phases/02-clientabletype-filter/02-01-SUMMARY.md`
</output>
