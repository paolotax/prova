---
phase: 02-clientabletype-filter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/documenti_controller.rb
autonomous: true

must_haves:
  truths:
    - "Controller permits clientable_type parameter"
    - "Filter receives clientable_type from request params"
  artifacts:
    - path: "app/controllers/documenti_controller.rb"
      provides: "clientable_type in FILTER_PARAMS"
      contains: ":clientable_type"
  key_links:
    - from: "app/controllers/documenti_controller.rb"
      to: "app/models/filters/documento/fields.rb"
      via: "FilterScoped concern uses FILTER_PARAMS"
      pattern: "FILTER_PARAMS.*clientable_type"
---

<objective>
Add clientable_type to DocumentiController FILTER_PARAMS

Purpose: Allow clientable_type to pass through strong parameters to the filter
Output: Updated FILTER_PARAMS constant including :clientable_type
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/controllers/documenti_controller.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clientable_type to FILTER_PARAMS</name>
  <files>app/controllers/documenti_controller.rb</files>
  <action>
  Modify app/controllers/documenti_controller.rb:

  Update the FILTER_PARAMS constant to include :clientable_type.

  Current:
  ```ruby
  FILTER_PARAMS = [:anno, :consegnati, :pagati, terms: [], causali: [], statuses: [], tipi_pagamento: []].freeze
  ```

  Updated (add :clientable_type as a scalar param, alongside :anno, :consegnati, :pagati):
  ```ruby
  FILTER_PARAMS = [:anno, :consegnati, :pagati, :clientable_type, terms: [], causali: [], statuses: [], tipi_pagamento: []].freeze
  ```

  Note: :clientable_type is a scalar parameter (single value), NOT an array.
  Place it with other scalar params before the array params.
  </action>
  <verify>
  Check the constant value:
  ```bash
  docker exec -it prova-app-1 bin/rails runner "
    puts DocumentiController::FILTER_PARAMS.inspect
    puts 'includes clientable_type: ' + DocumentiController::FILTER_PARAMS.include?(:clientable_type).to_s
  "
  ```
  Should output the array including :clientable_type and print true.
  </verify>
  <done>
  - FILTER_PARAMS includes :clientable_type
  - Parameter is permitted as scalar (not array)
  </done>
</task>

</tasks>

<verification>
- Controller loads without syntax errors
- FILTER_PARAMS.include?(:clientable_type) returns true
</verification>

<success_criteria>
DocumentiController::FILTER_PARAMS includes :clientable_type
</success_criteria>

<output>
After completion, create `.planning/phases/02-clientabletype-filter/02-03-SUMMARY.md`
</output>
