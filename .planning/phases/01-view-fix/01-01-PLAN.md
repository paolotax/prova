---
phase: 01-view-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/documenti/index.html.erb
autonomous: true

must_haves:
  truths:
    - "Filter form submits to documenti_path (not appunti_path)"
    - "Filter partial receives filter_type 'documento' (not 'appunto')"
  artifacts:
    - path: "app/views/documenti/index.html.erb"
      provides: "Corrected filter settings partial render"
      contains: "filter_url: documenti_path"
  key_links:
    - from: "app/views/documenti/index.html.erb"
      to: "filters/settings partial"
      via: "render call with filter_url parameter"
      pattern: "filter_url:\\s*documenti_path"
---

<objective>
Fix the filter settings partial render in documenti/index.html.erb to point to correct endpoints.

Purpose: The filter form currently submits to appunti_path instead of documenti_path, causing filter operations to redirect to the wrong controller. This breaks the filter functionality on the documenti index page.

Output: Corrected view file with proper filter_url, no_filtering_url, and filter_type values.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/views/documenti/index.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix filter_url, no_filtering_url, and filter_type in documenti index view</name>
  <files>app/views/documenti/index.html.erb</files>
  <action>
    Update lines 41-45 in app/views/documenti/index.html.erb.

    Change FROM:
    ```erb
    <%= render "filters/settings",
          filter_url: appunti_path,
          user_filtering: @user_filtering,
          no_filtering_url: appunti_path,
          filter_type: "appunto" %>
    ```

    Change TO:
    ```erb
    <%= render "filters/settings",
          filter_url: documenti_path,
          user_filtering: @user_filtering,
          no_filtering_url: documenti_path,
          filter_type: "documento" %>
    ```

    Three values need correction:
    1. filter_url: appunti_path -> documenti_path
    2. no_filtering_url: appunti_path -> documenti_path
    3. filter_type: "appunto" -> "documento"

    Keep the user_filtering: @user_filtering line unchanged.
  </action>
  <verify>
    Run: grep -A4 'render "filters/settings"' app/views/documenti/index.html.erb

    Expected output should show:
    - filter_url: documenti_path
    - no_filtering_url: documenti_path
    - filter_type: "documento"

    No occurrences of "appunti_path" or "appunto" should appear in this render block.
  </verify>
  <done>
    The filters/settings partial in documenti/index.html.erb renders with:
    - filter_url pointing to documenti_path
    - no_filtering_url pointing to documenti_path
    - filter_type set to "documento"
  </done>
</task>

</tasks>

<verification>
1. Grep verification:
   ```bash
   grep -n "appunti_path\|appunto" app/views/documenti/index.html.erb
   ```
   Should return NO matches (the file should not reference appunti anywhere in filter settings)

2. Positive verification:
   ```bash
   grep -A4 'render "filters/settings"' app/views/documenti/index.html.erb
   ```
   Should show documenti_path for both URLs and "documento" for filter_type
</verification>

<success_criteria>
- [ ] filter_url parameter is documenti_path
- [ ] no_filtering_url parameter is documenti_path
- [ ] filter_type parameter is "documento"
- [ ] No references to appunti_path or "appunto" in the filter settings render block
</success_criteria>

<output>
After completion, create `.planning/phases/01-view-fix/01-01-SUMMARY.md`
</output>
